import { UpdatePasswordForm } from "@/components/update-password-form";

export const metadata = { title: "Nueva contraseña | OMNI Scale" };

export default function Page() {
  return <UpdatePasswordForm />;
}
